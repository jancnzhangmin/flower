<script>
    // var request = $.ajax({
    //     url: 'http://yjmh.dh.cx/',
    //     type: 'POST',
    //     dataType: 'json',
    //     // data: $('#home_query').serialize(),
    //     data: {
    //         action: 'home_query',
    //         query_str: '陈薇茗',
    //     }
    // });
    //
    // request.done(function(ret) {
    //
    //     if (ret.count < 1) {
    //         $('#query_message').text(ret.message).removeClass('hidden');
    //     } else if (ret.count == 1) {
    //         window.location.href = ret.data[0];
    //     } else {
    //         $('#query_select').empty().removeClass('hidden');
    //         for (a = 0; a < ret.data.length; a++) {
    //             var text = ret.data[a].text;
    //             var url = ret.data[a].url;
    //             $('#query_select').append('<a href="' + url + '" class="input selection">' + text + '</a>');
    //         }
    //     }
    // });
</script>
<p id="notice"><%= notice %></p>

<h1>Tests</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Age</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tests.each do |test| %>
      <tr>
        <td><%= test.name %></td>
        <td><%= test.age %></td>
        <td><%= link_to 'Show', test %></td>
        <td><%= link_to 'Edit', edit_test_path(test) %></td>
        <td><%= link_to 'Destroy', test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Test', new_test_path %>

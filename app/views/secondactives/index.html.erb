<%= render 'layerbegin' %>

<div class="page-content">
  <div class="page-header">
    <h1>
      二级分润
    </h1>
  </div><!-- /.page-header -->

  <div class="row">
    <div class="col-xs-12">
      <!-- PAGE CONTENT BEGINS -->

      <div class="row">
        <div class="col-xs-12">
          <div class="table-responsive">
            <table id="sample-table-1" class="table table-striped table-bordered table-hover">
              <thead>
              <tr>
                <th class="text-center">活动名称</th>
                <th class="text-center">活动开始时间</th>
                <th class="text-center">活动结束时间</th>
                <th class="text-center">一级分润比（%）</th>
                <th class="text-center">二级分润比（%）</th>
                <th class="text-center">长期有效</th>
                <th class="text-center">状态</th>
                <th class="text-center" width="250px">操作</th>
              </tr>
              </thead>
              <tbody>
              <% @secondactives.each do |secondactive| %>
                <tr>
                  <td>
                    <%= secondactive.name %>
                  </td>
                  <td>
                    <%= secondactive.begintime ? secondactive.begintime.strftime('%Y-%m-%d %H:%M:%S') : '' %>
                  </td>
                  <td>
                    <%= secondactive.endtime ? secondactive.endtime.strftime('%Y-%m-%d %H:%M:%S') : '' %>
                  </td>
                  <td>
                    <%= secondactive.firstprofit %>
                  </td>
                  <td>
                    <%= secondactive.secondprofit %>
                  </td>
                  <td>
                    <% if secondactive.long == 1 %>
                        <i class="fa fa-check green"></i>
                    <% end %>
                  </td>
                  <td>
                    <% if secondactive.status == 1 %>
                    <span class="green">有效</span>
                      <% else %>
                      <span class="grey">停用</span>
                      <% end %>
                  </td>
                  <td>
                    <div class="visible-md visible-lg hidden-sm hidden-xs btn-group" data-turbolinks="false">
                      <a class="btn btn-xs btn-primary" href=<%= edit_secondactive_path(secondactive) %>>
                        <i class="fa fa-edit bigger-120"></i>
                        编辑
                      </a>
                      <a class="btn btn-xs btn-info" href=<%= secondactive_secondactivedetails_path(secondactive) %>>
                        <i class="fa fa-edit bigger-120"></i>
                        活动产品（<%= secondactive.secondactivedetails.count %>）
                      </a>
                      <a data-confirm="确认删除?" data-method="delete" class="btn btn-xs btn-danger" href=<%= secondactive_path(secondactive) %>>
                        <i class="fa fa-trash bigger-120"></i>
                        删除
                      </a>
                    </div>
                  </td>

                </tr>
              <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <div class="clearfix form-actions">
        <div class="col-md-12 text-right" data-turbolinks="false">
          <a class="btn btn-info" href=<%= new_secondactive_path %>>
            <i class="fa fa-plus bigger-110"></i>
            新增
          </a>
        </div>
      </div>






<%= render 'layerend' %>